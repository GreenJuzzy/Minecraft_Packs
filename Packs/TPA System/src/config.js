export const config = {
    cooldownTime: 30, // Seconds
    timeToAccept: 60, // The time the other person has to accept the request before it expires
    prefix: "-",
    suffix: "§f[§6TPA§f]",
    tpaDisabledTag: "tpaDisabled",
    plingSound: true,
    adminTag: "admin"
}